# Real-time Russian Speech Recognition with GigaAM
# 
# PyTorch. Install a CUDA-enabled build for automatic GPU inference on Windows.
torch>=2.0.0
torchaudio>=2.0.0

# Audio capture
sounddevice>=0.4.6

# Data processing
numpy>=1.24.0

# Console formatting
colorama>=0.4.6

# GigaAM - Install separately from the official repo:
#   git clone https://github.com/salute-developers/GigaAM.git
#   cd GigaAM
#   pip install -e .
#
# System dependency (install via Homebrew on macOS):
#   brew install ffmpeg


